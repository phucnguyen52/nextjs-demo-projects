import RegisterForm from "@/app/(auth)/register/register-form";

const RegisterPage = () => {
  return (
    <div>
      <h1 className="text-3xl font-semibold text-center mb-2">Đăng kí</h1>
      <RegisterForm />
    </div>
  );
};

export default RegisterPage;
